[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  SECRETS_SCAN_ENABLED = "false"

# ─── SPA fallback — must be LAST ───
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200